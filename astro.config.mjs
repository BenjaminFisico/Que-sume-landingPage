import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://benjaminfisico.github.io/Que-sume-landingPage/',
    base: '/Que-sume-landingPage',
});
